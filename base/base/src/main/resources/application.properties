# ==============================================================
# = CONFIGURACIÓN LOCAL (Tu computadora)
# ==============================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/Tiendita
spring.datasource.username=postgres
spring.datasource.password=CESaR1023

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuración de queries de seguridad
spring.queries.users-query=SELECT email, contrasena, activo FROM usuario WHERE email=?
spring.queries.roles-query=SELECT u.email, r.nombre FROM usuario u INNER JOIN rol_usuario ru ON u.id_usuario = ru.id_usuario INNER JOIN rol r ON ru.id_rol = r.id_rol WHERE u.email=?

server.port=8080
logging.level.com.exa.base=DEBUG
spring.thymeleaf.cache=false